{% extends 'base.html.twig' %}

{% block title %}Modification du mot de passe{% endblock %}

{% block body %}
<div class="container text-secondary">
        <h2 class="mb-5 mt-3">Modifier mon mot de passe</h2>
        <div class="row ">
            <div class="col-8 mx-auto">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-warning mt-5">Modifier le mot de passe</button>
                {{ form_end(form) }}
            </div>
        </div>
</div>
{% endblock %}
